<nav class="navbar navbar-expand-lg navbar-light" style="background-color: #e3f2fd;">
  <div class="container-fluid">
  <%= link_to "Home", root_path, class:"navbar-brand" %>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Users", users_path, class:"nav-link active" %>
        </li>
        <li class="nav-item">
          <%= link_to "Categories.", categories_path, class:"nav-link active" %>
        </li>
        <li class="nav-item">
          <%= link_to "Articles", articles_path, class:"nav-link active" %>
        </li>
      </ul>
      <% if logged_in? %>
      <ul class="d-flex navbar-nav mb-2 mb-lg-0">
        <% if current_user.admin? %>
            <li class="nav-item">
              <%= link_to "New category", new_category_path, method: :delete, class:"nav-link active" %>
            </li>
        <% end %>
          <li class="nav-item">
            <%= link_to "New Article", new_article_path, class:"nav-link active" %>
          </li>
          <li class="nav-item">
          <span class="nav-link active">User: <%= "(Admin)" if current_user.admin? %> <b>@<%= current_user.username %></b>.</span>
          </li>
          <li class="nav-item">
            <%= link_to "Edit profile", edit_user_path(current_user), class:"nav-link active" %>
          </li>
          <li class="nav-item">
            <%= link_to "Logout", logout_path, method: :delete, class:"nav-link active" %>
          </li>
      </ul>
      <% else %>
      <ul class="d-flex navbar-nav mb-2 mb-lg-0">
          <li class="nav-item">
            <%= link_to "Log in", login_path, class:"nav-link active" %>
          </li>    
          <li class="nav-item">
            <%= link_to "Sign up", signup_path, class:"nav-link active" %>
          </li>      
        </ul>
        <% end %>
    </div>
  </div>
</nav>
